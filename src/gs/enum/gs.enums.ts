export enum HeadRow {
    PEOPLE = 'Люди',
    LOGIN = 'Логин',
    PASSWORD = 'Пароль',
    DATE_AFTER = 'После даты',
    DATE_BEFORE = 'До даты',
    IMPORTANCE = 'Важность',
    STATUS = 'Статус',
}

export enum TableStatus {
    WORK,
    IMPOTANCE
}